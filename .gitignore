/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store

# Third-party Miyoo cross-toolchain clone. Fetch with:
#   git clone https://github.com/shauninman/union-miyoomini-toolchain.git \
#       cross/union-miyoomini-toolchain
# Build the base image from there, then `podman build` cross/Dockerfile.rust.
cross/union-miyoomini-toolchain/
